# robots.txt
# Production: Allow all crawlers
# Preview: Disallow all (set via middleware or environment)

User-agent: *
Disallow: /admin/
Disallow: /api/

# Allow preview environments to be blocked via environment variable
# Set PREVIEW_DISALLOW_ROBOTS=true in Vercel preview environment
