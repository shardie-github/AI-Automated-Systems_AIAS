# Patches Directory

This directory contains patches for npm packages applied using [patch-package](https://github.com/ds300/patch-package).

**Note:** All patches have been removed as dependencies have been updated to latest versions that no longer require patching.
