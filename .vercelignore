# Vercel ignore file
# Prevent unnecessary files from being deployed

# Development files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build artifacts (handled by Vercel)
# Note: We only ignore root .next, NOT apps/web/.next (which is the actual build output)
.next
out
dist
build
# DO NOT ignore apps/web/.next - that's the build output Vercel needs!

# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage
.nyc_output
*.log

# IDE
.vscode
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Documentation (optional - remove if you want docs deployed)
docs/
*.md
!README.md

# Scripts that shouldn't be deployed (but Vercel needs vercel-*.sh for build)
# Note: We don't ignore scripts/ entirely because Vercel needs vercel-ignore.sh and vercel-install.sh
tests/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
